{
  "name": "onyx-data-intelligence",
  "version": "3.2.0",
  "private": true,
  "description": "Onyx Data Intelligence — adaptive analytics and professional reporting platform, prepared for GitHub + Netlify continuous deployment.",
  "type": "module",
  "scripts": {
    "dev": "npm run build && node scripts/dev-server.mjs dist 5173",
    "check": "node scripts/check-project.mjs",
    "build": "node scripts/build.mjs",
    "preview": "node scripts/dev-server.mjs dist 4173"
  },
  "engines": {
    "node": ">=20"
  }
}
